# 사용자 친화적 여행 앱 톤 전환 수정안

작성일: 2026-05-09
대상 URL: `http://127.0.0.1:4177/`
검수 기준: WebKit Mobile `390 x 844`, Desktop `1280 x 900`

## 1. 목표

현재 1차 구현은 기능적으로는 맞지만, 사용자가 보는 첫인상이 `가족 여행 앱`보다 `운영표/관리 문서`에 가깝다. 2차 수정의 목표는 정보를 줄이는 것이 아니라, 사용자가 부담 없이 이해하고 행동할 수 있는 여행 앱 톤으로 바꾸는 것이다.

핵심 방향:

- 첫 화면은 `판단표`가 아니라 `지금 할 일`부터 보여준다.
- 문장과 라벨은 설계자 언어가 아니라 가족 여행자 언어로 바꾼다.
- 색은 경고/관리 느낌을 줄이고, 밝고 안정적인 휴양지 톤으로 정리한다.
- CTA는 한 화면에서 무엇을 눌러야 하는지 명확하게 만든다.
- 대안은 “실패 대비책”이 아니라 “무리하지 않는 선택”으로 표현한다.

## 2. 현재 화면의 문제

### 문제 1: 첫 카드가 너무 무겁다

현재 첫 카드가 `바나힐 진행 여부`이고, 내부가 `진행 / 중단 / 대안`으로 구성되어 있다. 논리적으로는 맞지만, 첫 화면에서 사용자가 느끼는 감정은 “여행 기대감”보다 “결정해야 할 숙제”에 가깝다.

첫 화면의 첫 카드는 아래 질문에 답해야 한다.

```text
지금 내가 뭘 하면 되지?
```

현재 첫 카드는 아래 질문에 답한다.

```text
바나힐을 어떤 조건에서 진행/중단하지?
```

두 번째 질문은 중요하지만 첫 번째 화면의 첫 질문은 아니다.

### 문제 2: 색이 경고와 문서 느낌을 만든다

현재 조합:

- 베이지 배경
- 진한 청록 버튼
- 주황 테두리 결정 카드
- 민트 칩 다수

이 조합은 부드러운 여행 앱보다 내부 관리 도구처럼 보인다. 특히 주황 테두리의 첫 카드는 사용자가 “위험/주의” 상황으로 읽기 쉽다.

### 문제 3: 버튼/칩/탭이 모두 비슷하다

탭, 기준 칩, 장소 칩, 액션 버튼이 모두 둥근 사각형 계열이라 위계가 약하다. 화면을 훑었을 때 “주요 행동”과 “설명 라벨”이 분리되지 않는다.

### 문제 4: 카피가 사용자보다 설계자 관점이다

현재 표현:

- `현장용 모바일 대시보드`
- `다음 결정`
- `바나힐 진행 여부`
- `진행 / 중단 / 대안`
- `컨디션 대안`

이 표현들은 정확하지만 친근하지 않다. 가족 여행자가 보는 화면에서는 더 직접적이고 부드러운 말이 필요하다.

## 3. 새 디자인 컨셉

컨셉명: `편안한 가족 여행 플랜`

원하는 인상:

- 밝다
- 복잡하지 않다
- 아이와 함께 무리하지 않는 느낌
- 지금 해야 할 일이 바로 보인다
- 대안이 있어서 안심된다

피해야 할 인상:

- 프로젝트 관리 대시보드
- 회의용 의사결정표
- 경고/리스크 보드
- 조사 리포트
- 항목이 많은 행정 문서

## 4. 모바일 첫 화면 재설계

### 현재 순서

```text
여행명
기준 칩
탭
바나힐 결정표
출발 전 체크
대안 카드
숙소 흐름
```

### 변경 순서

```text
여행명
출발 전에 이것만
무리하지 않는 선택
다음에 정할 것
탭 또는 하단 내비
```

상단 탭은 첫 화면에서 너무 강하다. 모바일에서는 하단 내비게이션을 주 탐색으로 두고, 상단 탭은 더 작게 만들거나 제거하는 것을 권장한다. 현재 구조상 제거가 부담되면, 상단 탭을 `compact segment`처럼 낮은 높이와 약한 색으로 만든다.

### 모바일 첫 화면 와이어프레임

```text
┌──────────────────────────────┐
│ 다낭 가족여행                │
│ 7.24-7.29 · 성인 2 · 아이 2  │
│ [더위 고려] [숙소 중심] [아이]│
└──────────────────────────────┘

┌──────────────────────────────┐
│ 출발 전에 이것만              │
│ 항공편·수하물·조식 조건을     │
│ 먼저 확인하면 됩니다.         │
│                              │
│ 남은 확인 15개                │
│ [준비 체크]                   │
└──────────────────────────────┘

┌──────────────────────────────┐
│ 무리하지 않는 선택            │
│ 더위·비·피곤한 날에는         │
│ 하얏트 수영장과 Camp Hyatt로  │
│ 가볍게 바꿀 수 있어요.        │
│ [쉬는 대안 보기]              │
└──────────────────────────────┘

┌──────────────────────────────┐
│ 다음에 정할 것                │
│ 바나힐은 전날 날씨 보고 결정  │
│ [판단 기준 보기]              │
└──────────────────────────────┘
```

핵심 변경:

- `바나힐 진행 여부` 카드를 첫 번째에서 세 번째로 내린다.
- `진행/중단/대안` 3열 판단표를 기본 노출하지 않는다.
- 첫 카드의 주 행동은 `준비 체크` 하나만 둔다.

## 5. 카드별 상세 설계

### A. 첫 카드: 출발 전에 이것만

역할: 사용자가 앱을 열자마자 해야 할 일을 알려준다.

카피:

```text
출발 전에 이것만
항공편·수하물·조식 조건을 먼저 확인하면 됩니다.
바나힐과 외부 일정은 전날 컨디션을 보고 정해도 괜찮아요.
```

표시 정보:

- 남은 확인 수
- 가장 중요한 확인 항목 2개
- `준비 체크` CTA

권장 UI:

```html
<article class="hero-task-card">
  <p class="eyebrow calm">출발 전에 이것만</p>
  <h2>항공편·수하물·조식 조건부터 확인</h2>
  <p>바나힐과 외부 일정은 전날 컨디션을 보고 정해도 괜찮아요.</p>
  <div class="mini-checks">
    <span>항공편명</span>
    <span>수하물</span>
    <span>호텔 조식</span>
  </div>
  <button>준비 체크</button>
</article>
```

### B. 두 번째 카드: 무리하지 않는 선택

역할: 대안을 부정적인 fallback이 아니라 안심 장치로 보여준다.

카피:

```text
무리하지 않는 선택
더위·비·피로가 있으면 리조트 안에서 충분히 쉬어도 좋은 일정입니다.
```

노출 후보:

- 하얏트 수영장·해변 시간
- Camp Hyatt
- 롯데마트/실내 쇼핑은 세 번째 정도로 낮춤

주의:

- `롯데마트 다낭`이 너무 먼저 보이면 여행 앱 감성이 약해진다.
- 첫 대안은 반드시 `하얏트 수영장·해변`처럼 여행 만족도가 있는 항목이어야 한다.

### C. 세 번째 카드: 다음에 정할 것

역할: 바나힐 판단을 보조 정보로 낮춘다.

카피:

```text
다음에 정할 것
바나힐은 7/26 밤에 날씨와 아이 컨디션을 보고 정하세요.
```

기본 노출:

- 날짜
- 한 줄 기준
- 버튼: `판단 기준 보기`

접힘/상세에서 노출:

- 가도 좋아요
- 쉬는 게 좋아요
- 대신 이렇게

현재의 `진행 / 중단 / 대안`은 아래처럼 바꾼다.

| 현재 | 변경 |
| --- | --- |
| 진행 | 가도 좋아요 |
| 중단 | 쉬는 게 좋아요 |
| 대안 | 대신 이렇게 |

## 6. 색상 재설계

현재 색은 문서/관리 느낌이 강하므로 아래 팔레트로 바꾼다.

```css
:root {
  --bg: #fafaf7;
  --surface: #ffffff;
  --surface-soft: #f3f8f7;
  --surface-warm: #fff8ef;
  --ink: #1f2428;
  --muted: #667085;
  --line: #e8e3da;
  --primary: #147c86;
  --primary-strong: #0b555d;
  --primary-soft: #e7f4f3;
  --warm: #e08a3e;
  --warm-soft: #fff3e6;
  --green-soft: #eef7f0;
  --blue-soft: #edf7fb;
  --shadow-soft: 0 10px 28px rgba(31, 36, 40, 0.07);
}
```

적용 규칙:

- 전체 배경은 `--bg`.
- 기본 카드는 흰색.
- 첫 카드 배경은 `--primary-soft` 또는 흰색 + 아주 약한 primary border.
- 주황색은 첫 카드 테두리에 쓰지 않는다.
- 주황색은 “날씨/주의” 같은 작은 라벨에만 쓴다.
- CTA는 `--primary`.
- 칩은 대부분 `--primary-soft`나 무채색으로 낮춘다.

금지:

```css
.decision-hero {
  background: #fff8f0;
  border-color: orange 계열;
}
```

권장:

```css
.hero-task-card {
  border: 1px solid rgba(20, 124, 134, 0.18);
  background: linear-gradient(180deg, #ffffff 0%, #f3f8f7 100%);
}
```

## 7. 컴포넌트 위계

### 버튼

한 카드에 filled 버튼은 1개만 둔다.

```css
.button-primary {
  background: var(--primary);
  color: #fff;
  border: 0;
}

.button-secondary {
  background: transparent;
  color: var(--primary-strong);
  border: 1px solid var(--line);
}

.button-quiet {
  background: transparent;
  border: 0;
  color: var(--primary-strong);
}
```

현재처럼 `지도`, `상세`, 탭, 칩이 모두 비슷한 무게로 보이면 안 된다.

### 칩

기준 칩은 작고 조용하게:

```css
.criteria-chips .chip {
  min-height: 26px;
  padding: 0 9px;
  border: 0;
  background: var(--primary-soft);
  color: var(--primary-strong);
  font-size: 12px;
}
```

### 탭

상단 탭은 모바일에서 지나치게 강하지 않게 한다.

```css
.tab-bar {
  gap: 4px;
  margin-bottom: 12px;
  padding: 4px;
  border-radius: 10px;
  background: rgba(255, 255, 255, 0.72);
}

.tab-button {
  min-height: 36px;
  border: 0;
  background: transparent;
  color: var(--muted);
}

.tab-button.is-active {
  background: var(--primary);
  color: #fff;
}
```

## 8. 카피 교체표

| 현재 | 변경 |
| --- | --- |
| 현장용 모바일 대시보드 | 가족 여행 플랜 |
| 다음 결정 | 다음에 정할 것 |
| 바나힐 진행 여부 | 바나힐은 전날 정하기 |
| 진행 | 가도 좋아요 |
| 중단 | 쉬는 게 좋아요 |
| 대안 | 대신 이렇게 |
| 컨디션 대안 | 무리하지 않는 선택 |
| 더위·비·피로 시 교체 | 더위·비·피곤한 날엔 이렇게 |
| 출발 전 체크 | 출발 전에 이것만 |
| 항공·숙소·수하물부터 확정 | 항공편·수하물·조식 조건부터 확인 |
| 후보 보기 | 장소 보기 |

문장 톤:

- `~합니다`만 반복하지 않는다.
- 가족에게 말하듯 짧고 부드럽게 쓴다.
- 단, 과장된 감탄문은 쓰지 않는다.

권장 문장:

```text
바나힐은 전날 날씨와 아이 컨디션을 보고 정해도 괜찮아요.
더위가 강하면 리조트 안에서 쉬는 일정으로 바꾸세요.
호이안은 식사를 먼저 하고 21시 전후로 복귀하는 흐름이 안정적입니다.
```

## 9. 장소 카드 톤 수정

현재 장소 카드가 정보 비교표에 가깝다면, 여행 앱에서는 `가야 하는 이유`와 `무리하지 않을 조건`을 먼저 보여준다.

카드 구조:

```text
[추천] [오전 또는 16시 이후]
하얏트 수영장·해변 시간
더운 시간에는 외부 이동보다 리조트 안에서 쉬는 게 가장 안정적입니다.

좋은 점: 아이들이 바로 즐길 수 있음
주의: 한낮 직사광선

[지도] [상세]
```

카피 규칙:

- `주의`만 강조하지 말고 `좋은 점`을 같이 둔다.
- 대안 카드의 제목은 `fallback` 느낌보다 `선택지` 느낌을 준다.
- 롯데마트 같은 실용 장소는 첫 화면에서는 낮은 우선순위로 둔다.

## 10. 데스크톱 레이아웃 수정

현재 데스크톱은 2컬럼이지만 첫 컬럼이 결정표라 무겁다. 아래처럼 바꾼다.

```text
좌측:
1. 출발 전에 이것만
2. 일정 미리보기
3. 숙소 흐름

우측:
1. 무리하지 않는 선택
2. 다음에 정할 것
3. 가족 메모
```

비율:

```css
@media (min-width: 960px) {
  .today-grid {
    grid-template-columns: minmax(0, 1.15fr) minmax(340px, 0.85fr);
  }
}
```

우측 카드가 보조 정보이므로 첫 결정표가 좌측 대형 카드로 오면 안 된다.

## 11. 메인 세션 작업 순서

메인 세션은 아래 순서로 수정한다.

1. `renderTodayPanel`의 섹션 순서를 바꾼다.
   - 현재: next decision → current state → fallback
   - 변경: current state → fallback → next decision

2. `current-state-card`를 `hero-task-card` 성격으로 강화한다.
   - 첫 화면의 대표 카드로 만든다.
   - CTA는 `준비 체크` 하나를 primary로 둔다.

3. `decision-hero`를 `next-decision-lite`로 낮춘다.
   - 주황 배경/테두리를 제거한다.
   - 기본 노출은 한 줄 설명과 버튼만.
   - `진행/중단/대안`은 상세/접힘 안으로 이동하거나 문구를 순화한다.

4. `fallback-list`의 제목과 카피를 바꾼다.
   - `컨디션 대안` → `무리하지 않는 선택`
   - `더위·비·피로 시 교체` → `더위·비·피곤한 날엔 이렇게`

5. CSS 토큰을 새 팔레트로 교체한다.

6. 버튼/칩/탭 위계를 정리한다.

7. WebKit 390x844, 1280x900으로 다시 캡처한다.

## 12. 메인 세션 전달용 프롬프트

```text
DESIGN_APP_TONE_REVISION.md 기준으로 2차 디자인 수정을 진행해줘.

현재 1차 구현은 기능은 맞지만, 가족 여행 앱보다는 운영표/관리 문서처럼 보입니다.
목표는 사용자 친화적인 "편안한 가족 여행 플랜" 톤입니다.

핵심 수정:
1. 모바일 첫 화면 순서를 `여행명 -> 출발 전에 이것만 -> 무리하지 않는 선택 -> 다음에 정할 것`으로 바꿔주세요.
2. `바나힐 진행 여부` 결정표를 첫 카드에서 내리고, 보조 카드인 `다음에 정할 것`로 낮춰주세요.
3. `진행/중단/대안` 문구는 `가도 좋아요/쉬는 게 좋아요/대신 이렇게`로 순화하거나 접힘 상세로 옮겨주세요.
4. 첫 카드는 `출발 전에 이것만`으로 만들고, CTA는 `준비 체크` 하나만 primary로 둬주세요.
5. 색상은 베이지+주황 경고 느낌을 줄이고, 밝은 배경 + 부드러운 바다색 + 작은 따뜻한 포인트로 바꿔주세요.
6. 탭/칩/버튼의 시각 위계를 분리해주세요. 한 카드에 filled 버튼은 1개만 둡니다.
7. 데스크톱은 좌측에 실사용 과업, 우측에 대안/다음 결정/메모가 오게 재배치해주세요.

검수:
- WebKit 390x844 첫 화면에서 3초 안에 "지금 뭘 하면 되는지"가 보여야 합니다.
- 첫 화면이 결정표나 관리 보드처럼 보이면 실패입니다.
- 주황색은 경고처럼 보이지 않게 최소화합니다.
- 하단 내비게이션과 카드가 겹치지 않아야 합니다.
```

