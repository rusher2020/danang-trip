# 2차 수정 디자인 QA 및 3차 보정 지시

작성일: 2026-05-10
검수 URL: `http://127.0.0.1:4177/`
브라우저: Playwright WebKit

## 검수 결과 요약

2차 수정은 방향이 좋아졌다. `후보`, `대안`, `판단 기준` 같은 관리형 용어가 화면에서 대부분 제거됐고, 첫 화면이 `바나힐 결정표`가 아니라 `출발 전에 이것만`으로 시작한다. 사용자가 지적한 “여행자 친화적이어야 한다”는 방향에 가까워졌다.

하지만 아직 최종은 아니다. 현재 화면은 여행 앱 톤으로 넘어가는 중간 단계이며, 특히 `가볼 곳` 탭과 장소 카드가 여전히 분석 카드처럼 무겁다.

검수 스크린샷:

- 오늘 탭 모바일: `.playwright-cli/page-2026-05-10T01-06-35-830Z.png`
- 오늘 탭 데스크톱: `.playwright-cli/page-2026-05-10T01-06-53-788Z.png`
- 가볼 곳 탭 모바일 상단: `.playwright-cli/page-2026-05-10T01-07-20-988Z.png`
- 가볼 곳 카드 스크롤: `.playwright-cli/page-2026-05-10T01-07-43-919Z.png`

## 좋아진 점

- 첫 진입 화면이 `출발 전에 이것만`으로 시작한다.
- `오늘 / 일정 / 가볼 곳 / 준비` 내비게이션은 이전보다 여행 앱에 가깝다.
- `가볍게 보내기`, `쉬운 일정`, `가볼 곳` 같은 표현은 방향이 맞다.
- 주황 경고 카드 느낌이 줄고, 첫 카드가 부드러운 톤으로 바뀌었다.
- 모바일 하단 내비게이션은 4열로 정상 유지된다.

## 남은 문제

### 1. 첫 카드 CTA가 2개라 초점이 흐리다

현재 첫 카드 하단:

```text
[준비 체크] [가볼 곳 보기]
```

첫 카드는 `출발 전에 이것만`이므로 주 행동은 `준비 체크` 하나여야 한다. `가볼 곳 보기`는 세 번째 `가볼 곳` 카드로 이동한다.

수정:

```text
[준비 체크]
```

### 2. `가볍게 보내기`가 카드처럼 묶이지 않는다

모바일 첫 화면에서 `가볍게 보내기` 섹션은 제목과 문장만 먼저 보이고, 실제 장소 카드가 아래로 잘린다. 그래서 하나의 친절한 여행 안내 카드라기보다 다음 섹션 제목처럼 보인다.

수정 방향:

- `가볍게 보내기`를 하나의 요약 카드로 만든다.
- 장소 3개를 모두 긴 카드로 바로 노출하지 말고, 작은 가로/세로 미니 카드 2개만 먼저 보여준다.

권장 구조:

```text
가볍게 보내기
더위·비·피곤한 날엔 리조트 안에서 쉬어도 좋아요.

[하얏트 수영장] [Camp Hyatt]
[쉬운 일정 보기]
```

### 3. `다음에 정할 일`은 아직 정보가 많다

오늘 탭의 `다음에 정할 일` 카드에 `가기 좋은 날 / 쉬는 게 나은 날 / 이렇게 바꾸기`가 모두 펼쳐져 있다. 2차 방향은 맞지만, 첫 화면에서는 다시 판단표처럼 느껴질 수 있다.

수정:

- 오늘 탭 기본 노출은 한 문장과 버튼만 둔다.
- 세부 조건은 `자세히 보기`를 눌렀을 때 sheet에서 보여준다.

기본 카드:

```text
다음에 정할 일
바나힐은 7/26 밤에 날씨와 아이 컨디션을 보고 정하세요.
[자세히 보기]
```

상세 sheet:

```text
가기 좋은 날
쉬는 게 나은 날
이렇게 바꾸기
```

### 4. `PLACES` 영어 eyebrow가 남아 있다

`가볼 곳` 탭 상단에 `PLACES`가 보인다. 여행 앱 톤에서는 영어 시스템 라벨이 어색하다.

수정:

```text
PLACES → 아이와 가기 좋은 곳
```

또는 eyebrow를 제거한다.

### 5. 가볼 곳 요약 박스가 행정 요약처럼 보인다

현재:

```text
꼭 가볼 곳 10
쉬운 일정 9
다음에 정할 일 바나힐은 전날...
```

이 박스는 다시 대시보드 느낌을 만든다. 여행 앱에서는 숫자 요약보다 사용자의 탐색을 도와야 한다.

수정 방향:

```text
오늘 보기 좋은 곳
하얏트 · 호이안 · 시내 식사부터 볼 수 있어요.
```

또는 요약 박스를 삭제하고 검색/필터를 바로 보여준다.

### 6. 장소 카드가 아직 너무 길고 분석적이다

현재 장소 카드에는 목록 단계에서 다음이 모두 노출된다.

- 아이 포인트
- 좋은 시간 · 소요
- 하얏트 기준 이동
- 주의
- 결정성 문장
- 한국인 후기
- 지도 열기
- 자세히
- 출처 보기

이건 여행 앱의 장소 목록이 아니라 상세 리포트에 가깝다. 목록 카드에서는 5초 안에 장소를 고를 수 있어야 한다.

목록 카드에서 남길 것:

```text
카테고리/상태 칩 2개
장소명
한 줄 설명
좋은 시간
아이와 좋은 점
[지도] [상세]
```

상세 sheet로 보낼 것:

```text
주의
이동 상세
한국인 후기
출처
긴 설명
추천 슬롯
```

권장 카드:

```text
[놀거리] [꼭 가볼 곳]
하얏트 수영장·해변 시간
더운 시간에는 밖으로 나가지 않고 리조트에서 쉬기 좋아요.

좋은 시간: 오전 또는 16시 이후
아이와: 수영장·해변·낮잠을 한 번에

[지도] [상세]
```

### 7. `출처 보기`가 목록 카드에 남아 있다

출처는 여행자에게 주 행동이 아니다. 목록 카드에서는 제거하고 상세 sheet 하단으로 이동한다.

### 8. 하단 내비게이션이 카드 내용을 시각적으로 덮는다

콘텐츠 자체는 스크롤되지만, 스크롤 중 카드 하단 버튼/본문과 하단 내비가 겹쳐 보이는 순간이 있다. `main` 하단 패딩을 더 키우고, 카드 리스트 마지막에 충분한 여백을 둔다.

권장:

```css
main {
  padding-bottom: calc(128px + env(safe-area-inset-bottom));
}
```

## 3차 수정 우선순위

1. 첫 카드 CTA를 `준비 체크` 하나로 줄인다.
2. `가볍게 보내기`를 미니 카드형 요약 섹션으로 만든다.
3. `다음에 정할 일`의 세부 조건을 sheet로 이동한다.
4. `PLACES` 영어 라벨을 제거한다.
5. `가볼 곳` 요약 박스를 삭제하거나 여행 앱 문구로 바꾼다.
6. 장소 목록 카드를 50% 이상 압축한다.
7. 목록 카드에서 `출처 보기`를 제거한다.
8. 모바일 하단 여백을 키운다.

## 메인 세션 전달용 프롬프트

```text
DESIGN_QA_ROUND2.md 기준으로 3차 보정을 진행해줘.

2차 수정은 방향은 좋아졌지만, 아직 가볼 곳 탭과 장소 카드가 분석 리포트처럼 무겁습니다.

수정 목표:
- 여행자가 5초 안에 "지금 무엇을 보면 되는지" 이해하게 만들기
- 장소 목록은 평가표가 아니라 여행 장소 탐색처럼 만들기
- 자세한 후기/주의/출처는 상세 sheet로 보내기

필수 수정:
1. 첫 카드 CTA는 `준비 체크` 하나만 남겨줘. `가볼 곳 보기`는 첫 카드에서 제거.
2. `가볍게 보내기`는 긴 장소 리스트가 아니라 요약 카드 + 미니 장소 2개 + `쉬운 일정 보기` 버튼으로 바꿔줘.
3. `다음에 정할 일`의 `가기 좋은 날/쉬는 게 나은 날/이렇게 바꾸기`는 기본 카드에서 숨기고 상세 sheet로 옮겨줘.
4. `가볼 곳` 탭의 `PLACES` 영어 라벨을 제거하거나 `아이와 가기 좋은 곳`으로 바꿔줘.
5. `가볼 곳 요약` 3칸 숫자 박스는 삭제하거나 여행 앱 문구형 안내로 바꿔줘.
6. 장소 목록 카드는 `장소명, 한 줄 설명, 좋은 시간, 아이와 좋은 점, 지도/상세`만 남기고 나머지는 상세 sheet로 옮겨줘.
7. 목록 카드의 `출처 보기`는 제거하고 상세 sheet 하단으로 이동해줘.
8. 모바일에서 하단 내비가 카드 내용을 덮어 보이지 않도록 하단 여백을 더 키워줘.

검수 기준:
- `가볼 곳` 탭이 후보 평가표나 리포트처럼 보이면 실패.
- 장소 카드 하나가 모바일에서 너무 길게 이어지면 실패.
- 목록에서 `출처 보기`가 보이면 실패.
- 첫 화면 첫 카드는 CTA가 하나만 보여야 한다.
```

