# 디자인 QA 전달 메모

작성일: 2026-05-09

## 1차 구현 반영

- 첫 진입 탭을 `오늘`로 변경했습니다.
- 기존 주 탐색의 `후기` 탭을 제거하고 `오늘 / 일정 / 장소 / 준비` 4탭 구조로 정리했습니다.
- `오늘` 탭에 다음 항목을 추가했습니다.
  - 다음 결정 카드
  - 출발 전 체크 카드
  - 더위/비/피로 시 대안 카드
  - 확정 숙소 흐름
- 하단 내비게이션을 4열로 수정했습니다.
- 장소 카드 액션 순서를 `지도 열기 -> 자세히 -> 출처 보기`로 바꿨습니다.
- 상태 라벨을 `확정 / 추천 / 조건부 / 대안 / 제외` 중심으로 표시하도록 매핑했습니다.
- 상태별 pill 색상 클래스를 추가했습니다.
- 430px 이하에서 카드 액션과 체크리스트가 과하게 눌리지 않도록 보정했습니다.
- 960px 이상에서 `오늘` 탭은 2컬럼 보드로 표시되도록 했습니다.

## 2차 여행자 친화 수정

`TRAVELER_FRIENDLY_REDESIGN.md`를 `DESIGN_HANDOFF.md`보다 우선해서 반영했습니다.

- 첫 화면 순서를 `출발 전에 이것만 -> 가볍게 보내기 -> 가볼 곳 -> 다음에 정할 일` 흐름으로 조정했습니다.
- 주 탐색 라벨을 `오늘 / 일정 / 가볼 곳 / 준비`로 바꿨습니다.
- 화면 문구에서 `후보`, `대안`, `판단 기준`, `진행/중단` 계열 표현을 걷어냈습니다.
- `액티비티`, `맛집` 표현을 화면에서는 `놀거리`, `식사/식당` 중심으로 바꿨습니다.
- 장소 데이터의 주요 설명도 `선택지`, `쉬운 선택`, `가볍게 해결`처럼 여행자 친화적인 표현으로 정리했습니다.
- 첫 카드의 주황 경고 느낌을 줄이고, 부드러운 teal 계열 카드로 바꿨습니다.

## 검증 결과

- `node scripts/validate.js` 통과
- `node --check script.js` 통과
- 현재 데이터 기준: 18개 JSON, 49개 장소

## 브라우저 검수 상태

Playwright Chromium을 사용자 영역에 설치한 뒤 캡처를 완료했습니다.

캡처 파일:

- Mobile 390 x 844: `output/playwright/danang-mobile-v2.png`
- Desktop 1280 x 900: `output/playwright/danang-desktop-v2.png`
- Traveler-friendly mobile: `output/playwright/danang-mobile-friendly.png`
- Traveler-friendly desktop: `output/playwright/danang-desktop-friendly.png`

참고: 시스템 Chrome 설치는 sudo 비밀번호가 필요해 진행하지 않았고, Playwright 전용 Chromium으로 검수했습니다.

따라서 디자인 QA 세션에서는 실제 브라우저에서 아래를 우선 확인해 주세요.

- 모바일 390 x 844에서 첫 화면 순서가 `여행명 -> 다음 결정 -> 출발 전 체크 -> 대안/확정 흐름`으로 보이는지
- 하단 내비게이션이 한 줄 4칸인지
- 하단 내비게이션이 마지막 카드 텍스트를 덮지 않는지
- `오늘` 탭의 대안 카드에서 `지도`와 `상세` 버튼이 정상 동작하는지
- 장소 카드의 `지도 열기 -> 자세히 -> 출처 보기` 순서가 현장 사용성에 맞는지
- 1280 x 900에서 `오늘` 탭이 2컬럼 보드처럼 보이는지

## 다음 컨텐츠 보강 후보

- 이스타항공 항공편명/예약번호/수하물 포함 여부를 입력할 빠른 카드 구조
- 다낭 공항 공식 사이트 기반 귀국일 터미널/체크인/대기시간 확인 링크
- 하얏트 내부 프로그램의 체크인 당일 확인 항목
- 장소별 `현장 판단 한 줄`: 지금 가도 되는지, 언제 포기할지, 대안은 무엇인지

## 2차 디자인 QA: 구조와 감성 재작업 필요

검수 기준:

- URL: `http://127.0.0.1:4177/`
- Browser: Playwright WebKit
- Mobile: `390 x 844`
- Desktop: `1280 x 900`
- Mobile screenshot: `.playwright-cli/page-2026-05-09T05-58-56-323Z.png`
- Desktop screenshot: `.playwright-cli/page-2026-05-09T05-59-09-233Z.png`

결론:

1차 구현은 기능 요구사항은 꽤 반영했지만, 사용자 친화성과 시각 디자인은 아직 부족하다. 현재 화면은 가족 여행자가 쓰는 앱이라기보다 여행 설계자가 만든 운영표처럼 보인다. 사용자는 “무엇을 결정해야 하는지”를 알 수는 있지만, 화면이 편안하거나 직관적으로 다가오지 않는다.

### 핵심 문제

#### 1. 첫 카드가 너무 무겁고 행정적이다

현재 첫 카드:

- `다음 결정`
- `바나힐 진행 여부`
- `진행 / 중단 / 대안`
- 긴 조건 문장 3개

이 구조는 논리적으로는 맞지만, 모바일 첫 화면의 감정적 진입점으로는 차갑다. 가족 여행자가 첫 화면에서 보고 싶은 것은 “회의용 판단표”가 아니라 `오늘/출발 전 무엇을 하면 되는지`와 `무리하지 않아도 되는 대안`이다.

권장 변경:

- 첫 카드를 `오늘의 한 줄` 또는 `출발 전 우선순위` 카드로 바꾼다.
- 바나힐 판단표는 첫 카드가 아니라 두 번째 또는 접힌 카드로 내린다.

권장 첫 카드 카피:

```text
출발 전 우선순위
항공편·수하물·조식 조건만 먼저 확인하면 됩니다.
바나힐은 전날 날씨와 아이 컨디션을 보고 결정하세요.

[준비 체크] [바나힐 판단 보기]
```

#### 2. 정보 계층이 사용자 과업 중심이 아니다

현재 첫 화면은 다음 순서다.

```text
여행명
탭
바나힐 결정표
출발 전 체크
대안 카드
숙소 흐름
```

사용자 친화적으로는 다음 순서가 낫다.

```text
여행명
지금 할 일 1개
안심 대안 2개
다음 큰 결정 1개
확정 흐름
```

즉, “결정표”보다 “지금 할 일”이 먼저 와야 한다.

#### 3. 색감이 여행 앱도, 운영 도구도 아닌 중간 상태다

현재 팔레트는 베이지 배경, 청록 버튼, 주황 테두리, 민트 칩이 섞여 있다. 각각은 나쁘지 않지만 합쳐지면 다음 문제가 생긴다.

- 베이지 배경이 낡은 문서 느낌을 준다.
- 청록 CTA가 너무 무겁고 반복되어 화면 전체가 딱딱하다.
- 주황 테두리의 결정 카드가 경고/주의처럼 보여 첫 진입 감정이 부담스럽다.
- 칩의 민트색이 많아져 정보가 모두 비슷한 중요도로 보인다.

권장 방향:

- 배경은 더 밝고 깨끗하게: `#FAFAF7` 또는 `#F8FAF9`
- Primary는 깊은 청록 대신 부드러운 바다색: `#0F766E` 또는 `#147C86`
- Warm은 경고 테두리보다 작은 포인트로만 사용: `#E08A3E`
- 카드 테두리는 거의 보이지 않게: `#E8E3D8`
- 첫 카드 배경은 주황빛이 아니라 아주 옅은 바다색/하늘색 계열로 전환

권장 토큰:

```css
:root {
  --bg: #fafaf7;
  --surface: #ffffff;
  --surface-soft: #f2f7f6;
  --ink: #1f2428;
  --muted: #6b7280;
  --line: #e7e2d8;
  --primary: #147c86;
  --primary-strong: #0b5258;
  --warm: #e08a3e;
  --soft-blue: #eaf6f7;
  --soft-green: #edf7f1;
  --caution: #9a5b1f;
}
```

#### 4. 버튼과 칩이 모두 비슷하게 생겨 행동 우선순위가 흐려진다

현재 화면은 탭, 칩, 카드 내부 버튼이 모두 둥근 사각형/알약 형태라 서로 구분이 약하다.

권장 변경:

- 상단 기준 칩은 더 작고 조용하게.
- 주요 CTA는 filled 버튼 1개만.
- 보조 CTA는 outline이 아니라 text/link 성격으로 낮춘다.
- 관련 장소 버튼은 칩처럼 보이게 유지하되, 첫 카드 안에서는 최대 2개만 표시한다.

#### 5. 카피가 사용자 언어보다 설계자 언어에 가깝다

예:

- `현장용 모바일 대시보드`
- `바나힐 진행 여부`
- `진행 / 중단 / 대안`
- `컨디션 대안`

권장:

- `가족 여행 플랜`
- `오늘은 무리하지 않는 날`
- `가도 되는 조건`
- `쉬는 대안`
- `아이들이 피곤하면`

더 친화적인 라벨:

| 현재 | 변경 |
| --- | --- |
| 현장용 모바일 대시보드 | 가족 여행 플랜 |
| 다음 결정 | 다음에 정할 것 |
| 바나힐 진행 여부 | 바나힐은 전날 정하기 |
| 진행 | 가도 좋아요 |
| 중단 | 쉬는 게 좋아요 |
| 대안 | 대신 이렇게 |
| 컨디션 대안 | 무리하지 않는 선택 |
| 출발 전 체크 | 출발 전에 이것만 |

## 2차 재설계안

### 모바일 첫 화면 재구성

```text
다낭 가족여행
2026.07.24-07.29 · 성인 2 · 아이 2
[더위 고려] [아이 컨디션] [숙소 중심]

┌──────────────────────────────┐
│ 출발 전에 이것만              │
│ 항공편·수하물·조식 조건을     │
│ 먼저 확인하면 됩니다.         │
│ [준비 체크]                   │
└──────────────────────────────┘

┌──────────────────────────────┐
│ 무리하지 않는 선택            │
│ 하얏트 수영장 · Camp Hyatt    │
│ 더위/비/피로하면 이쪽으로     │
│ [대안 보기]                   │
└──────────────────────────────┘

┌──────────────────────────────┐
│ 다음에 정할 것                │
│ 바나힐은 전날 날씨 보고 결정  │
│ 가도 좋아요 / 쉬는 게 좋아요  │
│ [판단 기준 보기]              │
└──────────────────────────────┘
```

핵심은 바나힐 판단을 없애는 것이 아니라, 사용자가 처음 보는 순간 부담스럽지 않게 순서를 바꾸는 것이다.

### 데스크톱 재구성

현재 데스크톱은 2컬럼이지만 양쪽 카드의 분위기가 여전히 딱딱하다. 데스크톱은 다음처럼 쓴다.

```text
좌측 60%:
- 출발 전에 이것만
- 전체 일정 미리보기
- 숙소 흐름

우측 40%:
- 무리하지 않는 선택
- 다음에 정할 것
- 가족 메모
```

결정표는 우측 보조 카드로 내려야 한다.

## 메인 세션에 전달할 2차 수정 지시

```text
1차 구현은 기능적으로 맞지만, 사용자 친화성과 색감이 부족합니다.
현재 화면은 가족 여행 앱보다 운영표/관리 문서처럼 보입니다.

2차 수정 목표:
- 첫 화면을 "바나힐 결정표"가 아니라 "출발 전에 이것만" 중심으로 바꿔주세요.
- 바나힐 판단은 첫 카드에서 내리고, "다음에 정할 것" 보조 카드로 낮춰주세요.
- 카피를 설계자 언어에서 사용자 언어로 바꿔주세요.
- 색감은 베이지+청록+주황 경고 느낌을 줄이고, 밝은 배경 + 부드러운 바다색 + 작은 따뜻한 포인트로 정리해주세요.
- 첫 카드의 주황 테두리/배경은 제거하고, 안정감 있는 soft blue/white 카드로 바꿔주세요.
- 주요 CTA는 한 카드당 1개만 filled로 두고, 나머지는 조용한 text/link 버튼으로 낮춰주세요.
- 모바일 첫 화면 순서는 `여행명 -> 출발 전에 이것만 -> 무리하지 않는 선택 -> 다음에 정할 것`이 되게 해주세요.
- 데스크톱에서는 좌측에 실사용 과업, 우측에 대안/결정/메모를 둔 2컬럼으로 정리해주세요.

검수 기준:
- 390x844 첫 화면이 부담스러운 결정표가 아니라 편안한 여행 플랜처럼 보여야 합니다.
- 첫 화면에서 "내가 지금 뭘 하면 되는지"가 3초 안에 이해되어야 합니다.
- 주황색은 경고처럼 보이지 않게 최소화해야 합니다.
- 버튼/칩/탭의 시각 위계가 구분되어야 합니다.
```
